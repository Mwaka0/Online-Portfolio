---
import MainLayout from "../../layouts/mainLayout.astro";

export async function getStaticPaths() {
    return [
        {   
            params: { projects: "proj1" }, 
            props: {
                name: "Project 1",
                desc: "some descrprition",
            },
        },
    ];
}

const { projects } = Astro.params;
const { name, desc } = Astro.props;
---

<MainLayout>
    <section class="mx-auto w-3/6">
        <img src=`/images/${projects}.png` width="600", height="600" class="rounded " />
        <h1 class="font-bold mt-4">Project {name}</h1>
        <p>{desc}</p>
        <section></section>
    </section>
</MainLayout>